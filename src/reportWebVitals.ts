// frontend/src/reportWebVitals.ts
export type ReportHandler = (onPerfEntry: any) => void;

const reportWebVitals = (_onPerfEntry?: ReportHandler) => {
  // 필요 없으면 비워둬도 됨
};

export default reportWebVitals;
